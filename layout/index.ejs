<div id="artical-list" class="ui container">
    <div class="ui divided items">
        <% site.posts.each(function(post){ %>
            <%- partial('_partial/index-item', {
                post: post,
                index: true
            }) %>
        <% }) %>
    </div>
</div>
